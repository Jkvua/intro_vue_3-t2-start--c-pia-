//const product = 'Socks'

const app = Vue.createApp({
    data() {
        return {
            product: 'Boots',
            description: 'Meias para aquecer os pés'
        }
    }

})

//const app = Vue.createApp({Options Object})